const User = require('./User');
const Project = require('./Project');
const Vehicle = require('./Vehicle');
const Transaction = require('./Transaction')

module.exports = { User, Project, Vehicle, Transaction };
